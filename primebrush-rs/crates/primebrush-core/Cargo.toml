[package]
name = "primebrush-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "PrimeBrush core traits and plugin registry (Rust migration scaffold)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
