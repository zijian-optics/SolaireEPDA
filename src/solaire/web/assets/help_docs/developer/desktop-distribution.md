# Windows 桌面分发（开发视角）

本文用于记录桌面交付规划，便于后续实现时对齐目标。

## 目标

- 提供 Windows 桌面安装包。
- 支持授权机制与环境检测引导。
- 保持离线可用与可诊断性。

## 建议架构

- 桌面壳负责：进程管理、系统交互、安装引导、授权输入。
- 后端负责：授权校验、环境检测、业务 API。

## 实施关注点

- 授权校验应在后端裁决，前端仅负责展示和触发。
- 环境检测需结构化输出，便于 UI 呈现与日志追踪。
- 升级与回滚路径需明确，避免破坏用户项目目录。
